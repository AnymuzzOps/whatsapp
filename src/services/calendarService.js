async function createCalendarEvent(eventData = {}) {
  // Stub para futura integración con Google Calendar.
  console.info('Creación de evento de calendario pendiente de implementación:', eventData);
  return { created: false, eventData };
}

module.exports = {
  createCalendarEvent,
};
